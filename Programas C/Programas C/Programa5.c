#include<stdio.h>
#include<stdlib.h>
enum logico {falso,verdadero};
enum logico Interruptor;

int main()
{
    Interruptor=falso;
    printf("El valor Interruptor vale: %d\n\n\n\n\n",Interruptor);
    system("pause");
    return 0;
}
